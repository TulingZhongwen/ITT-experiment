
```markdown
# ITT实验验证指南（阶段三）：方向规则操作化规范

版本：2.0
依据：ITT预印本v1.0（DOI：）
开源协作：欢迎提交Issue/PR，详见GitHub仓库
适用对象：已完成阶段一（模拟验证）和阶段二（公开数据验证）的研究者

---

## 1. 总体目标

验证ITT**预言三（方向规则）**：系统自发运动方向与张力梯度之间存在系统性的符号关系。

**核心主张：** 自反力 $F_{\text{self}}$ 在统计意义上存在——系统不是随机运动，而是与内部张力方向存在可预测的符号关系。

**零假设：** 一致性比例 = 50%（随机）

**ITT点预测：** 一致性比例 > 75%

---

## 2. 双轨道设计

阶段三采用**双轨道**设计，根据数据质量自动选择：

| 轨道 | 适用条件 | ∇U估计方法 | 风险 |
|:---|:---|:---|:---|
| **主轨道** | 低维（$N_{\text{eff}} \geq 100d$） | Silverman核密度 + 局部线性回归 | 无额外假设 |
| **简化轨道** | 高维降级或数据质量不足 | 假设 $\nabla U \propto \vec{\Delta}$ | $\Pi(s) \in \mathcal{E}$ 可能不成立 |

**自动降级条件：** 当 $N_{\text{eff}} = N / 2^d < 100d$ 时，主轨道自动降级为简化轨道。

---

## 3. 主轨道操作步骤

### 3.1 输入数据

- `traj`: (T, d) 状态空间轨迹（Takens嵌入后）
- `original_signal`: (T_raw,) 原始一维时间序列（用于计算互信息块长度）
- `delta`: float 最小可分辨距离（来自Θ计算）

### 3.2 核密度估计（Silverman带宽）

```python
from itt_core import gradient_u_local_linear

# 自动计算Silverman带宽
gradU, p, h_silverman = gradient_u_local_linear(traj, h=None, epsilon=1e-6)
```

**带宽敏感性分析：** 扫描 $h \times \{0.1, 0.3, 1.0, 3.0, 10.0\}$，验证一致性比例是否稳健。

### 3.3 局部线性回归估计∇U

对每个轨迹点 $s(t)$：
1. 找 $k$ 个近邻（默认 $k = \max(2d, 30)$）
2. 加权最小二乘拟合 $\log p(s')$
3. 岭回归正则化：$\lambda = \epsilon \cdot \lambda_{\max}(X^T W X)$

```python
# 正则化敏感性分析
for eps in [1e-8, 1e-6, 1e-4]:
    gradU_test, _, _ = gradient_u_local_linear(traj, h=h_silverman, epsilon=eps)
    # 计算一致性比例...
```

### 3.4 方向规则一致性计算

```python
# Δ⃗(s) 来自 compute_pi_and_delta()
dot = np.sum(Delta_unit * gradU, axis=1)  # Δ⃗ · ∇U
sigma = np.sign(dot).astype(int)  # +1: 叛逆, -1: 顺从
R = np.mean(sigma > 0)  # 叛逆比例
```

### 3.5 块状Bootstrap统计检验

```python
from itt_core import mutual_info_block_length, block_bootstrap

# 块长度 = 互信息第一极小值
L = mutual_info_block_length(original_signal)

# Bootstrap
R_star, ci_lower, ci_upper = block_bootstrap(sigma, L, n_bootstrap=1000)
```

**检验决策：**
- 若 $0.50 < \text{ci\_lower}$：拒绝零假设，预言三被支持
- 否则：预言三未被支持

### 3.6 效应量分层

| R范围 | 分层 | 解读 |
|:---|:---|:---|
| > 0.75 | 强支持 | ITT核心预测成立 |
| [0.60, 0.75] | 弱支持 | 自反力存在，效应量低于预期 |
| [0.50, 0.60] | 边缘 | 可能自反力微弱，或数据质量不足 |
| < 0.50 | 证伪 | 明确证伪 |

---

## 4. 简化轨道操作步骤

当主轨道不可用时自动切换。

### 4.1 假设

$$\nabla U(s) \propto \vec{\Delta}(s)$$

即：自我印象 $\Pi(s)$ 就是安态。

### 4.2 一致性计算

在此假设下，$\vec{\Delta} \cdot \nabla U > 0$ 恒成立，系统恒叛逆。

$$R \approx 1.0$$

### 4.3 风险标注

> 简化轨道依赖于工作假设 $\Pi(s) \in \mathcal{E}$。若主轨道可用，应优先采信主轨道结果。两轨道结论分歧表明 $\Pi(s) \in \mathcal{E}$ 可能不成立。

---

## 5. 预言四（可选）：自我印象即安态

嵌套在预言三主轨道的分析流程中，不增加额外计算成本。

### 5.1 检验方法

在计算方向规则时，额外记录 $\sigma_U(t) = \text{sgn}(\vec{\Delta} \cdot \nabla U)$。

**零假设：** $\sigma_U$ 随机（50%正，50%负）

**备择假设：** $P(\sigma_U > 0) > 0.5$（$\vec{\Delta}$ 与 $\nabla U$ 显著同向）

### 5.2 统计检验

使用与预言三相同的块状Bootstrap流程。

### 5.3 与核心预言的关系

预言四的证伪**不影响**预言三的结论。预言三只要求方向规则成立，不要求 $\nabla U$ 的方向恰好与 $\vec{\Delta}$ 一致。

---

## 6. 参数汇总

| 参数 | 默认值 | 来源/依据 |
|:---|:---|:---|
| Silverman带宽 $h$ | $(4/(d+2))^{1/(d+4)} \sigma N^{-1/(d+4)}$ | 标准非参数统计 |
| 近邻数 $k$ | $\max(2d, 30)$ | 经验，确保局部线性 |
| 岭回归 $\epsilon$ | $10^{-6}$ | 相对于最大特征值 |
| 块长度 $L$ | 互信息第一极小值 | 数据驱动 |
| Bootstrap次数 $B$ | 1000 | 标准 |
| 高维降级阈值 | $N_{\text{eff}} = N/2^d < 100d$ | 经验 |

---

## 7. 验证数据集建议

| 数据集 | 模态 | 状态标签 | 适合检验 |
|:---|:---|:---|:---|
| 模拟数据（Lorenz系统） | 仿真 | 正面对照/负面对照 | 算法正确性 |
| OpenNeuro ds002718 | EEG | 清醒/麻醉 | 预言三（清醒状态） |
| OpenNeuro ds003478 | EEG | 清醒/N1/N2/N3/REM | 预言三（清醒 vs 睡眠） |

**注意：** 预言三需在 $\Lambda > \Lambda_{\text{noise}}$ 的状态上检验（意识涌现条件满足）。麻醉或深睡状态不满足此条件，方向规则无定义。

---

## 8. 常见问题与调试

| 问题 | 可能原因 | 解决方案 |
|:---|:---|:---|
| 主轨道自动降级 | 嵌入维数过高或轨迹点数不足 | 检查 $N_{\text{eff}}$，接受简化轨道或增加数据 |
| 一致性比例接近0.5 | 数据噪声过大或状态空间结构不佳 | 检查预处理，降低嵌入维数 |
| Bootstrap CI过宽 | 块长度过短或数据自相关过强 | 检查互信息第一极小值是否合理 |
| 带宽敏感性不稳健 | 密度估计不稳定 | 增大近邻数 $k$ 或检查数据分布 |
| 预言四与预言三矛盾 | $\Pi(s) \in \mathcal{E}$ 不成立 | 报告分歧，优先采信主轨道 |

---

## 9. 结果报告模板

完整的阶段三分析应输出：

1. **轨道选择：** 主轨道/简化轨道，降级原因（如触发）
2. **一致性比例：** $R$ 值
3. **统计检验：** 块状Bootstrap 95% CI，零假设拒绝情况
4. **效应量分层：** 强支持/弱支持/边缘/证伪
5. **敏感性分析：** 带宽扫描结果、正则化扫描结果
6. **预言四（可选）：** 是否支持 $\Pi(s) \in \mathcal{E}$
7. **图表：** Bootstrap分布直方图、效应量分层条形图

---

维护者：图灵中文
许可证：MIT
```
